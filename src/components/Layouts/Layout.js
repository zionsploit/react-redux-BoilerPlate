import React from 'react';
import PagesRouting from '../Routes/Pages.Routes';

const Layout = () => {
    return (
        <div>
            <PagesRouting />
        </div>
    );
};

export default Layout;